'use client';
import { useEffect, useState } from 'react';
import Link from 'next/link';
import { Plus, Package } from 'lucide-react';
import { SectionShell } from '@/components/section-shell';
import { apiRequest } from '@/lib/api';
type Product={id:string;name:string;description:string|null;category:string|null;status:string;analysis:unknown};
export default function ProductsPage(){const [products,setProducts]=useState<Product[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{apiRequest<{products:Product[]}>('/products').then(x=>setProducts(x.products)).catch(e=>setError(e instanceof Error?e.message:'Unable to load products')).finally(()=>setLoading(false));},[]);return <SectionShell active="/products" eyebrow="Catalog" title="Products" description="Manage products and turn them into customer conversations." action={<Link href="/products/add" className="rounded-xl bg-emerald-600 px-4 py-2 text-sm font-bold text-white"><Plus className="mr-1 inline size-4"/>Add product</Link>}>{loading?<p>Loading products…</p>:error?<div role="alert" className="rounded-xl bg-rose-50 p-4 text-rose-700">{error}</div>:products.length===0?<div className="rounded-2xl border border-dashed bg-white p-8 text-center"><Package className="mx-auto text-emerald-600"/><p className="mt-3 font-bold">No products yet</p><p className="mt-1 text-sm text-slate-500">Add your first product to start product intelligence.</p></div>:<div className="grid gap-4">{products.map(p=><Link href={`/products/${p.id}`} key={p.id} className="rounded-2xl border bg-white p-4 shadow-sm"><div className="flex gap-3"><div className="grid size-12 place-items-center rounded-xl bg-emerald-50 text-emerald-700"><Package/></div><div><h2 className="font-bold">{p.name}</h2><p className="mt-1 text-sm text-slate-500">{p.description||p.category||'Product'}</p><span className="mt-3 inline-block text-xs font-bold text-emerald-700">{p.status}</span></div></div></Link>)}</div>}</SectionShell>}
