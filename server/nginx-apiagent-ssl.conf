upstream sales_api {
    server 127.0.0.1:4400;
    keepalive 64;
}

server {
    listen 80;
    server_name apiagent.akilimatic.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    http2 on;
    server_name apiagent.akilimatic.com;

    ssl_certificate /etc/letsencrypt/live/apiagent.akilimatic.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/apiagent.akilimatic.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    access_log /var/log/nginx/apiagent-access.log;
    error_log /var/log/nginx/apiagent-error.log;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # Health checks
    location /health/live {
        proxy_pass http://sales_api/health/live;
        proxy_set_header Host $host;
        access_log off;
    }

    location /health/ready {
        proxy_pass http://sales_api/health/ready;
        proxy_set_header Host $host;
        access_log off;
    }

    # API routes
    location /api/v1 {
        proxy_pass http://sales_api/api/v1;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-ID $request_id;
        proxy_set_header X-Forwarded-Host $server_name;
        proxy_set_header X-Forwarded-Port $server_port;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
        proxy_connect_timeout 75s;
        proxy_buffer_size 128k;
        proxy_buffers 4 256k;
        proxy_busy_buffers_size 256k;
    }

    # WebSocket support
    location /ws {
        proxy_pass http://sales_api/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 86400s;
    }
}
