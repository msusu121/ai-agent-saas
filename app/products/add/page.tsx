'use client'; import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { SectionShell } from '@/components/section-shell'; import { apiRequest } from '@/lib/api';
export default function AddProduct(){const r=useRouter();const [name,setName]=useState('');const [description,setDescription]=useState('');const [error,setError]=useState('');const [saving,setSaving]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setSaving(true);try{const x=await apiRequest<{product:{id:string}}>('/products',{method:'POST',body:JSON.stringify({name,description})});r.push(`/products/${x.product.id}`);}catch(e){setError(e instanceof Error?e.message:'Unable to create product')}finally{setSaving(false)}}return <SectionShell active="/products" eyebrow="Products" title="Add / Analyze Product" description="Describe what you sell and let BizHunter identify the right buyers."><form onSubmit={submit} className="mx-auto max-w-xl space-y-4 rounded-2xl border bg-white p-5"><input required value={name} onChange={e=>setName(e.target.value)} className="w-full rounded-xl border px-4 py-3" placeholder="Product or service name"/><textarea required minLength={20} value={description} onChange={e=>setDescription(e.target.value)} className="min-h-32 w-full rounded-xl border px-4 py-3" placeholder="Describe your product, buyers, and value"/>{error&&<p className="text-sm text-rose-600">{error}</p>}<button disabled={saving} className="w-full rounded-xl bg-emerald-600 px-4 py-3 font-bold text-white">{saving?'Saving…':'Save and analyze →'}</button></form></SectionShell>}
